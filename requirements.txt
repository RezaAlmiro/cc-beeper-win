fastapi>=0.110
uvicorn[standard]>=0.29
winotify>=1.1.0
pywin32>=306
keyboard>=0.13.5
python-dotenv>=1.0
requests>=2.31
psutil>=5.9
PySide6>=6.6
# Pillow is only used by install_launcher.bat to build the Windows .ico
# from a sprite. Widget runs fine without it — that step just gets
# skipped.
Pillow>=10.0
